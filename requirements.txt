# FiroGate — Python dependencies (only what the app directly uses)

# ─ Web framework ──
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.24
Jinja2==3.1.6

# ─ Data / validation ─
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.3.0
python-dotenv==1.2.2

# ─ Database (SQLite async) ─
SQLAlchemy==2.0.44
aiosqlite==0.21.0

# ─ Background jobs & logging ─
APScheduler==3.11.0
loguru==0.7.3

# ─ Security (auth, JWT, encryption, 2FA) ─
python-jose==3.5.0
bcrypt==4.1.3
cryptography==46.0.7
pyotp==2.9.0
qrcode==8.2
Pillow==12.2.0

# ─ HTTP clients ─
httpx==0.28.1
requests==2.33.1

# ─ Firebase Auth (hybrid auth integration) ─
firebase-admin==7.4.0
